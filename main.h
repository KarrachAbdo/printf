#ifndef MAIN_H
#define MAIN_H
#include <stdarg.h>
#include <unistd.h>
#include <string.h>
int print_char(char c);
int print_string(const char *str);
int _printf(const char *format, ...);

#endif /* MAIN_H */
